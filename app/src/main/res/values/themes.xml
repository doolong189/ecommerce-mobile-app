<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ECommerceApplication" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/background</item>
        <item name="android:statusBarColor">@color/toolbar</item>
    </style>

    <style name="Theme.ECommerceApplication" parent="Base.Theme.ECommerceApplication" />
</resources>